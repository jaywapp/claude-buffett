# 성능 및 안정성 설계

평가금액은 기존 sum을 유지한다. Python의 sum과 반복 +=는 부동소수점 누적 오차가 달라질 수 있어 루프 통합을 적용하지 않는다. DB 오류만 경고 및 기존 실패 코드 1로 변환한다. 투자 판단과 임계값은 변경하지 않으며 임시 DB unittest로 검증한다.

정상 입력의 반환값·순서·출력 의미를 보존한다. 실패를 성공으로 숨기지 않는다. 외부 API/운영 DB를 테스트에 사용하지 않는다. 새로운 기능/의존성은 추가하지 않는다.
